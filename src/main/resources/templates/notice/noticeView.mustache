<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <!-- favicon -->
    <link rel="apple-touch-icon" sizes="180x180" href="favicon_io/apple-touch-icon.png">
    <link rel="icon" type="image/png" sizes="32x32" href="favicon_io/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="favicon_io/favicon-16x16.png">

    <link rel="stylesheet" href="/css/notice/noticeView.css">
    <title>19시 :: 공지사항</title>
</head>
<body>
<div class="header">
    {{> common/header}}
</div>

<div class="page-wrap">
    <div class="text-box">
        <div>홈 > 게시판 > 공지사항</div>
        <div>공지사항</div>
    </div>

    <div class="board-box">
        <br><br>
        <table class="table write-box">
            <tr style="background-color: rgba(224, 224, 224, 0.5);">
                <th style="width: 100px;">제목</th>
                <td style="width: 570px;">{{n.nTitle}}</td>
                <th style="width: 100px;">작성일</th>
                <td>{{n.writeDate}}</td>
            </tr>
            <tr style="background-color: rgba(224, 224, 224, 0.5);">
                <th style="height: 30px; line-height: 40px;">첨부파일</th>
                <td colspan="3" style="line-height: 30px;">
                    {{#n.filename}}
                        <p>{{n.filename}}</p>
                    {{/n.filename}}
                    {{^n.filename}}
                        <p>첨부파일 없음</p>
                    {{/n.filename}}
                </td>
            </tr>
            <tr>
                <th class="noticeContent">내용</th>
                <td style="text-align: center;" colspan="3">
                    {{#n.filename}}
                        <img src="/upload/notice/{{n.filename}}" width="500px;">
                    {{/n.filename}}
                    <p>{{{n.nContent}}}</p>
                </td>
            </tr>
        </table>

        <div class="btn-left">
            <a class="btn-emp" href="/noticeListPage">목록으로</a>
        </div>
        <div class="btn-right">
            {{#isAdmin}}
                <form id="updateForm" method="post" action="/updateNoticeFrm?nNo={{n.nNo}}" style="display:inline-block;">
                <input type="hidden" name="nNo" value="{{n.nNo}}">
                <button class="btn btn-custom" type="submit">수정하기</button>
                </form>

                <form id="deleteForm" method="post" action="/deleteNotice?nNo={{n.nNo}}" style="display:inline-block; margin-right: 10px;">
                <input type="hidden" name="nNo" value="{{n.nNo}}">
                <button class="btn btn-custom" type="submit">삭제하기</button>
                </form>
            {{/isAdmin}}
        </div>
    </div>
</div>

<br><br><br><br>

<footer>
    {{> common/footer}}
</footer>
</body>
</html>
